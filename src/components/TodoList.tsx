import { useState } from 'react';

function TodoList() {
  return (
    <div>
      <ul>
        <li>Be a Trillionaire</li>
        <button>Remove</button>
        <button>Edit</button>
      </ul>
    </div>
  );
}

export default TodoList;
